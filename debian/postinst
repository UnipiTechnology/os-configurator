#!/bin/sh -e

# Post-inst maintainer script of os-configurator package
# See Readme of the package for info about licensing.


case "$1" in
  configure)
    dpkg-trigger os-configurator-force
    ;;
  triggered)
    if [ -x /usr/sbin/os-configurator ]; then
      echo "Calling os-configurator update..." >&2
      ( /usr/sbin/os-configurator --force && dpkg-trigger os-configurator-done ) || echo "os-configurator return error, please call it manually."
    fi
  ;;
esac

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.
#DEBHELPER#
exit 0
